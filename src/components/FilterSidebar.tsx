import type { ProductSort } from '../api/storefront'
type Props={collapseLabel?:string;expandLabel?:string;onExpandedChange?:(expanded:boolean)=>void;search:string;category:string;sort:ProductSort;categories:readonly string[];searchPlaceholder:string;allCategoriesLabel:string;sortLabel:string;newestSortLabel:string;priceLowSortLabel:string;priceHighSortLabel:string;clearLabel:string;selectedColors:readonly string[];minRating:number;colorOptions:readonly string[];ratingLabel:string;colorsLabel:string;benefits?:{title:string;items:readonly string[]};onSearch:(v:string)=>void;onCategory:(v:string)=>void;onSort:(v:ProductSort)=>void;onClear:()=>void;onColorToggle:(v:string)=>void;onRating:(v:number)=>void}
export function FilterSidebar({search,category,sort,categories,searchPlaceholder,allCategoriesLabel,sortLabel,newestSortLabel,priceLowSortLabel,priceHighSortLabel,clearLabel,selectedColors,minRating,colorOptions,ratingLabel,colorsLabel,benefits={title:'Shopping with us',items:['Thoughtfully selected goods','Support when you need it','Secure checkout']},onSearch,onCategory,onSort,onClear,onColorToggle,onRating}:Props){return <aside className="filter-sidebar expanded" aria-label="Catalog filters"><div className="filter-sidebar-heading"><p className="eyebrow">Filter</p>{Boolean(search||category||sort!=='newest'||selectedColors.length||minRating)&&<button className="clear-filters" type="button" onClick={onClear}>{clearLabel}</button>}</div><div className="filter-fields"><label className="filter-field">{searchPlaceholder}<input type="search" value={search} onChange={e=>onSearch(e.target.value)} placeholder={searchPlaceholder}/></label><label className="filter-field">{allCategoriesLabel}<select value={category} onChange={e=>onCategory(e.target.value)}><option value="">{allCategoriesLabel}</option>{categories.map(item=><option value={item} key={item}>{item}</option>)}</select></label><label className="filter-field">{sortLabel}<select value={sort} onChange={e=>onSort(e.target.value as ProductSort)}><option value="newest">{newestSortLabel}</option><option value="price-low">{priceLowSortLabel}</option><option value="price-high">{priceHighSortLabel}</option></select></label><fieldset className="filter-group"><legend>{ratingLabel}</legend>{[5,4,3,2,1].map(r=><label className="check-option" key={r}><input type="radio" name="rating" checked={minRating===r} onChange={()=>onRating(r)}/><span>{r === 5 ? '5 stars only' : `${r} stars & up`}</span></label>)}</fieldset><fieldset className="filter-group"><legend>{colorsLabel}</legend>{colorOptions.map(c=><label className="check-option" key={c}><input type="checkbox" checked={selectedColors.includes(c)} onChange={()=>onColorToggle(c)}/><span className={`color-swatch color-${c.toLowerCase()}`}/><span>{c}</span></label>)}</fieldset><div className="filter-benefits"><p>{benefits.title}</p><ul>{benefits.items.map(i=><li key={i}>{i}</li>)}</ul></div></div></aside>}
