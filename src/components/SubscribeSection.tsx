import { useNotification } from './NotificationProvider'
import { apiFetch } from '../api/http'
import { useState, type FormEvent } from 'react'
export function SubscribeSection(){const notify=useNotification();const [email,setEmail]=useState('');const [status,setStatus]=useState<'idle'|'loading'|'success'|'error'>('idle');const submit=async(event:FormEvent<HTMLFormElement>)=>{event.preventDefault();setStatus('loading');try{const response=await apiFetch('/api/newsletter/subscribe',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email})});const result=await response.json().catch(()=>null) as {emailSent?:boolean;error?:string}|null;if(!response.ok)throw new Error(result?.error||'Subscription failed');setStatus('success');setEmail('');notify(result?.emailSent?'You are subscribed. A confirmation email is on its way.':'You are subscribed. Configure the email sender to receive a confirmation message.', 'success')}catch(error){setStatus('error');notify(error instanceof Error?error.message:'We could not subscribe you right now. Please try again.')}};return <section className="subscribe-section page-section" aria-labelledby="subscribe-title"><div><p className="eyebrow">STAY IN THE LOOP</p><h2 id="subscribe-title">Good things, occasionally.</h2><p>New arrivals, thoughtful edits, and useful ideas delivered to your inbox.</p></div><form className="subscribe-form" onSubmit={submit}><label htmlFor="subscribe-email">Email address</label><div><input id="subscribe-email" type="email" required value={email} onChange={event=>{setEmail(event.target.value);setStatus('idle')}} placeholder="you@example.com" disabled={status==='loading'||status==='success'}/><button className="primary-button" type="submit" disabled={status==='loading'} onClick={()=>{if(status==='success'){setStatus('idle')}}}>{status==='loading'?'Joining…':status==='success'?'Subscribed':'Subscribe'} <span aria-hidden="true">→</span></button></div></form></section>}
