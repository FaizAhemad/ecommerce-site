import type { MouseEvent } from 'react'
import type { StorefrontApiResponse } from '../api/storefront'
import { ArrowIcon } from '../components/ArrowIcon'
import { VirtualizedProductGrid } from '../components/VirtualizedProductGrid'
import { SubscribeSection } from '../components/SubscribeSection'
type HomePageProps={storefront:StorefrontApiResponse;onNavigate:(path:string)=>(event:MouseEvent<HTMLAnchorElement>)=>void;onAdd:(productId:string)=>Promise<void>;onOpenProduct:(id:string)=>void}
export function HomePage({storefront,onNavigate,onAdd,onOpenProduct}:HomePageProps){const {hero,collection,story}=storefront.content;const currency=new Intl.NumberFormat(storefront.localization.locale,{style:'currency',currency:storefront.localization.currency,maximumFractionDigits:0});const featured=storefront.products.slice(0,8);const trending=storefront.products.slice(8,16);return <><section className="hero-section" aria-labelledby="hero-title"><div className="hero-copy"><p className="eyebrow">{hero.eyebrow}</p><h1 id="hero-title">{hero.title}</h1><p className="hero-text">{storefront.identity.tagline} {hero.description}</p><a className="primary-button" href="/products" onClick={onNavigate('/products')}>{hero.actionLabel} <ArrowIcon direction="right" /></a></div><div className="hero-art" aria-label={hero.artworkDescription} role="img"><div className="sun-disc"/><div className="hero-vase"/><div className="hero-cup"/><div className="hero-book"/><span className="art-label">{hero.artworkLabel}</span></div></section><section className="home-products page-section" aria-labelledby="featured-products-title"><div className="section-heading"><div><p className="eyebrow">Featured edit</p><h2 id="featured-products-title">Trending now</h2></div><p className="section-note">A considered selection of customer favorites and new arrivals.</p></div><VirtualizedProductGrid products={featured} currency={currency} addToCartLabel={collection.addToCartLabel} ratingLabel={collection.ratingLabel} reviewsLabel={collection.reviewsLabel} onAdd={onAdd} onOpenProduct={onOpenProduct} virtualize={false}/><a className="primary-button home-explore" href="/products" onClick={onNavigate('/products')}>Explore all products <ArrowIcon direction="right"/></a></section><section className="home-story page-section"><div><p className="eyebrow">{story.eyebrow}</p><h2>{story.title}</h2></div><p className="hero-text">{story.description}</p></section><section className="home-products page-section" aria-labelledby="latest-products-title"><div className="section-heading"><div><p className="eyebrow">Latest arrivals</p><h2 id="latest-products-title">Fresh for the everyday</h2></div></div><VirtualizedProductGrid products={trending} currency={currency} addToCartLabel={collection.addToCartLabel} ratingLabel={collection.ratingLabel} reviewsLabel={collection.reviewsLabel} onAdd={onAdd} onOpenProduct={onOpenProduct} virtualize={false}/><a className="primary-button home-explore" href="/products" onClick={onNavigate('/products')}>View the full collection <ArrowIcon direction="right"/></a></section><SubscribeSection/></>}
