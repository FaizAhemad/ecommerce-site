import { useState, type FormEvent, type MouseEvent } from 'react'
import type { StorefrontApiResponse } from '../api/storefront'
type Props={mode:'login'|'signup';storefront:StorefrontApiResponse;onNavigate:(path:string)=>(event:MouseEvent<HTMLAnchorElement>)=>void;onLogin:()=>void}
export function AuthPage({mode,storefront,onNavigate,onLogin}:Props){const signup=mode==='signup';const copy=storefront.content.auth;const [message,setMessage]=useState('');const submit=(event:FormEvent<HTMLFormElement>)=>{event.preventDefault();onLogin();setMessage(signup?copy.signupSuccess:copy.loginSuccess)};const google=()=>{onLogin();setMessage(copy.googleSuccess)};return <section className="page-section auth-page" aria-labelledby="auth-title"><div className="auth-card"><p className="eyebrow">{signup?copy.signupEyebrow:copy.loginEyebrow}</p><h1 id="auth-title">{signup?copy.signupTitle:copy.loginTitle}</h1><p className="hero-text">{signup?copy.signupDescription:copy.loginDescription}</p><button className="google-button" type="button" onClick={google}><span className="google-g">G</span> {copy.googleLabel}</button><div className="auth-divider"><span>{copy.emailDivider}</span></div><form className="auth-form" onSubmit={submit}>{signup&&<><label>{copy.fullNameLabel}<input required autoComplete="name" placeholder={copy.fullNameLabel} /></label><label>{copy.mobileLabel}<input required type="tel" autoComplete="tel" placeholder="+91 00000 00000" /></label><label>{copy.addressLabel}<textarea required autoComplete="street-address" rows={3} placeholder={copy.addressLabel} /></label></>}<label>{copy.emailLabel}<input required type="email" autoComplete="email" placeholder="you@example.com" /></label><label>{copy.passwordLabel}<input required type="password" autoComplete={signup?'new-password':'current-password'} minLength={8} placeholder={copy.passwordPlaceholder} /></label><button className="primary-button auth-submit" type="submit">{signup?copy.signupAction:copy.loginAction} <span aria-hidden="true">→</span></button></form>{message&&<p className="auth-message" role="status">{message}</p>}<p className="auth-switch">{signup?copy.signupSwitch:copy.loginSwitch}{' '}<a href={signup?'/login':'/signup'} onClick={onNavigate(signup?'/login':'/signup')}>{signup?copy.loginLink:copy.signupLink}</a></p><p className="auth-security">{copy.securityNote}</p></div></section>}
