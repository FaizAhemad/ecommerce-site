import { useEffect, useMemo, useState } from 'react'
import type { MouseEvent } from 'react'
import type { StorefrontApiResponse } from '../api/storefront'

type Props = { storefront: StorefrontApiResponse; productId: string; onAdd: () => void; onNavigate: (path: string) => (event: MouseEvent<HTMLAnchorElement>) => void }
const reviewCopy = ['Beautifully made and exactly as pictured.', 'A thoughtful product that fits perfectly into daily use.', 'The quality is excellent and delivery was smooth.', 'Simple, useful, and even better in person.']

export function ProductDetailPage({ storefront, productId, onAdd, onNavigate }: Props) {
  const product = storefront.products.find((item) => item.id === productId)
  const [showAll, setShowAll] = useState(false); const [limit, setLimit] = useState(4); const [reviewRating, setReviewRating] = useState(5); const [reviewComment, setReviewComment] = useState(''); const [submitted, setSubmitted] = useState(false); const [selected, setSelected] = useState<{ type: 'image' | 'video'; id: string } | null>(null)
  useEffect(() => { document.body.style.overflow = showAll ? 'hidden' : ''; return () => { document.body.style.overflow = '' } }, [showAll])
  const reviews = useMemo(() => product ? Array.from({ length: Math.min(product.reviewCount, 12) }, (_, i) => ({ id: `${product.id}-${i}`, rating: Math.max(1, Math.min(5, Math.round(product.rating + ((i % 3) - 1) * .3))), text: reviewCopy[i % reviewCopy.length], author: ['Aarav', 'Maya', 'Rohan', 'Isha'][i % 4], date: `${i + 1} ${i % 2 ? 'weeks' : 'months'} ago` })) : [], [product])
  if (!product) return <section className="page-section state-panel"><p className="eyebrow">{storefront.content.collection.eyebrow}</p><h1>{storefront.content.collection.noResultsLabel}</h1><a className="primary-button" href="/products" onClick={onNavigate('/products')}>{storefront.content.collection.addToCartLabel}</a></section>
  const initial = product.media.images.find((image) => image.isPrimary); const image = selected?.type === 'image' ? product.media.images.find((item) => item.id === selected.id) : initial; const video = selected?.type === 'video' ? product.media.videos.find((item) => item.id === selected.id) : undefined; const total = reviews.length || 1; const distribution = [5, 4, 3, 2, 1].map((rating) => ({ rating, count: reviews.filter((item) => item.rating === rating).length })); const currency = new Intl.NumberFormat(storefront.localization.locale, { style: 'currency', currency: storefront.localization.currency, maximumFractionDigits: 0 })
  return <><section className="detail-section page-section"><div className="detail-gallery"><div className={`detail-art product-art ${product.tone}`}><div className="detail-media-viewport">{image ? <img className="product-primary-image" src={image.url} alt={image.alt} /> : video ? <video className="product-primary-video" src={video.url} poster={video.posterUrl} controls playsInline /> : <div className="product-shape" />}</div></div><div className="media-rail">{product.media.images.map((item) => <button className="media-thumb" type="button" key={item.id} onClick={() => setSelected({ type: 'image', id: item.id })}><img src={item.url} alt={item.alt} /></button>)}</div></div><div className="detail-copy"><p className="eyebrow">{product.category}</p><h1>{product.name}</h1><p className="detail-price">{currency.format(product.price)}</p><p className="hero-text">{storefront.identity.tagline}</p><button className="primary-button" type="button" onClick={onAdd}>{storefront.content.collection.addToCartLabel} <span aria-hidden="true">+</span></button></div></section><section className="ratings-panel"><h2>Product ratings &amp; reviews</h2><div className="ratings-summary"><div className="average-rating"><strong>{product.rating.toFixed(1)}</strong><span>★</span><p>{product.reviewCount} ratings<br />{reviews.length} reviews</p></div><div className="rating-bars">{distribution.map(({ rating, count }) => <div className="rating-bar" key={rating}><span>{rating} ★</span><div><i style={{ width: `${count ? Math.max(count / total * 100, 8) : 0}%` }} /></div><b>{count}</b></div>)}</div></div><div className="latest-reviews"><div className="reviews-heading"><h3>Latest reviews</h3><button className="secondary-button" type="button" onClick={() => setShowAll(true)}>View all reviews</button></div>{reviews.slice(0, 3).map((review) => <article className="review-item" key={review.id}><div><strong>{'★'.repeat(review.rating)}</strong><span>{review.author} · {review.date}</span></div><p>{review.text}</p></article>)}</div></section>{showAll && <div className="review-overlay" onMouseDown={(event) => { if (event.target === event.currentTarget) setShowAll(false) }}><aside className="review-drawer" role="dialog" aria-modal="true" aria-labelledby="all-reviews-title"><button className="drawer-close" type="button" onClick={() => setShowAll(false)} aria-label="Close reviews">×</button><h2 id="all-reviews-title">All reviews</h2>{reviews.slice(0, limit).map((review) => <article className="review-item" key={review.id}><div><strong>{'★'.repeat(review.rating)}</strong><span>{review.author} · {review.date}</span></div><p>{review.text}</p></article>)}{limit < reviews.length && <button className="secondary-button" type="button" onClick={() => setLimit((value) => value + 4)}>Load more reviews</button>}</aside></div>}<section className="review-section"><p className="eyebrow">{storefront.content.collection.reviewsLabel}</p><h2>{storefront.content.reviews.title}</h2>{submitted ? <p className="review-success">{storefront.content.reviews.successLabel}</p> : <form className="review-form" onSubmit={(event) => { event.preventDefault(); setSubmitted(true) }}><label>{storefront.content.reviews.ratingLabel}<select value={reviewRating} onChange={(event) => setReviewRating(Number(event.target.value))}><option value="5">5 / 5</option><option value="4">4 / 5</option><option value="3">3 / 5</option><option value="2">2 / 5</option><option value="1">1 / 5</option></select></label><label>{storefront.content.reviews.commentLabel}<textarea value={reviewComment} onChange={(event) => setReviewComment(event.target.value)} required rows={5} /></label><button className="primary-button" type="submit" disabled={!reviewComment.trim()}>{storefront.content.reviews.submitLabel}</button></form>}</section></>
}
