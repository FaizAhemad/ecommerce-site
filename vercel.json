{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/[...route]?route=$1" },
    { "handle": "filesystem" },
    { "src": "/((?!api(?:/|$)|@|src/|node_modules/|assets/|.*\\.).*)", "dest": "/index.html" }
  ]
}
